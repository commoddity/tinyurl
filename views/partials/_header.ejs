<!DOCTYPE html>
<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if(username){ %>
      <div class="logged-in" style="padding-right: 10px; padding-left: 10px;">
      <p style="float: left; padding-right: 10px; vertical-align: middle;">Logged in as: <b><%= username %></b></p>
      <form style="float: right;" method="POST" action="/logout" id="user-logout">
        <button type="submit" id="logout-button" class="btn btn-secondary">Logout</button>
      </form>
    </div>
    <% } else{ %>
      <div class="user-login">
        <form method="POST" action="/login" id="user-login">
          <input type="text" id="username-text" name="username" placeholder="Enter username">
          <button type="submit" id="login-button" class="btn btn-secondary">Login</button>
        </form>
      </div>
   <% } %>
  </nav>
</header>